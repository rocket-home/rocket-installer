// Коды возврата rocket. Наружу их читает install.sh — по ним он решает, что сказать
// пользователю, поэтому таксономия важнее вкуса.
//
// Почему именно эти числа: 0 и 1 уже заняты прежним поведением («готово» / «шаг не выполнен»),
// 2 занимает сам make («recipe failed» — любой код рецепта он схлопывает в двойку), 13 — чужой
// код Node (ERR_UNSETTLED_TOP_LEVEL_AWAIT), которым мастер молча падал без терминала.
// 130 — общепринятое «прервано по SIGINT», 3 — ближайший свободный.
export const EXIT = {
  OK: 0,
  FAILED: 1,
  NO_TTY: 3,
  CANCELLED: 130,
};
